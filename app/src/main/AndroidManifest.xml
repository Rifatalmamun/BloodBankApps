<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rifat.bloodbankapps">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BeDonorActivity" />
        <activity android:name=".SearchBloodActivity" />
        <activity android:name=".BloodBankActivity" />
        <activity android:name=".AmbulanceActivity" />
        <activity android:name=".FactsActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ShareActivity" />
        <activity android:name=".SearchBloodResultShowActivity" />
        <activity android:name=".SingleResultShowActivity" />
        <activity android:name=".PostForBloodActivity"></activity>
    </application>

</manifest>